# From https://help.github.com/articles/dealing-with-line-endings, 9/1/12

# Set default behavior, in case users don't have core.autocrlf set.
* text=auto

# Explicitly declare text files we want to always be normalized (crlf => lf) and
# converted to native line endings on checkout.
#*.mo text
#*.mos text
#*.sh text
#*.py text
#package.order text

# Declare files that will always have CRLF line endings on checkout.
#*.bat text eol=crlf
#*.txt text eol=crlf
#*.c text eol=crlf
#*.h text eol=crlf
#*.m text eol=crlf

# Denote all files that are truly binary and should not be modified.
#*.jpg binary
#*.lib binary
#*.png binary
#*.pyc binary
#*.tar.gz binary
#*.xls binary
#*.zip binary
